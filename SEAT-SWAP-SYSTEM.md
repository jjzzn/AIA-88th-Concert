# ระบบสลับที่นั่งสำหรับคอนเสิร์ต - เอกสารสรุป

## 📋 สรุประบบ

ระบบสลับที่นั่งที่พัฒนาขึ้นสำหรับ AIA Concert Booking System ช่วยให้ Admin สามารถจัดการคำขอเปลี่ยนที่นั่งของผู้ใช้ได้อย่างปลอดภัยและมีประสิทธิภาพ โดยรักษา QR Code เดิมและอัปเดทข้อมูลแบบ real-time

## 🗂️ ไฟล์ที่สร้าง

### 1. Database Schema & Migration
- **`database-seat-swap-migration.sql`** - Migration script สำหรับเพิ่มตาราง `seat_swap_history` และฟังก์ชัน `swap_seat()`

### 2. TypeScript Types
- **`types/seat-swap.ts`** - Type definitions สำหรับระบบสลับที่นั่ง

### 3. Services
- **`lib/services/seatSwapService.ts`** - Service layer สำหรับจัดการ API calls และ business logic

### 4. UI Components
- **`components/admin/SeatSwapPortal.tsx`** - หน้าหลักของระบบสลับที่นั่ง
- **`components/admin/SeatMapSelector.tsx`** - Component แสดงผังที่นั่งและเลือกที่นั่งใหม่
- **`components/admin/SwapConfirmationDialog.tsx`** - Dialog ยืนยันการสลับที่นั่ง

## 🏗️ โครงสร้าง Database

### ตาราง `seat_swap_history`
```sql
- id: UUID (Primary Key)
- booking_seat_id: UUID (Foreign Key)
- booking_id: UUID (Foreign Key)
- old_seat_id: UUID (Foreign Key)
- new_seat_id: UUID (Foreign Key)
- swapped_by: UUID (Foreign Key to admin_users)
- swapped_at: TIMESTAMP
- reason: TEXT (Optional)
- admin_notes: TEXT (Optional)
- admin_ip: VARCHAR(45) (Optional)
- status: VARCHAR(20) ('success', 'failed', 'rollback')
- error_message: TEXT (Optional)
```

### ฟังก์ชัน `swap_seat()`
ฟังก์ชัน PostgreSQL ที่ทำการสลับที่นั่งแบบ atomic transaction:
- ตรวจสอบความถูกต้องของข้อมูล
- อัปเดท `booking_seats.seat_id` (เก็บ `qr_token` เดิม)
- อัปเดทสถานะที่นั่งเดิมและใหม่
- บันทึก audit log
- Rollback อัตโนมัติหากเกิดข้อผิดพลาด

### View `seat_swap_history_detailed`
View สำหรับดูประวัติการสลับที่นั่งพร้อมข้อมูลครบถ้วน

## 🔧 การติดตั้ง

### 1. รัน Database Migration
```bash
# เข้าสู่ Supabase SQL Editor
# Copy และรัน database-seat-swap-migration.sql
```

### 2. ตรวจสอบ Types
```bash
# TypeScript types ถูกสร้างไว้แล้วใน types/seat-swap.ts
```

### 3. เพิ่ม Route สำหรับ Seat Swap Portal
เพิ่มใน routing system:
```typescript
// ใน index.tsx หรือ routing file
if (currentPath === '/admin/seat-swap') {
  return <SeatSwapPortal adminUser={currentUser} />;
}
```

## 📱 การใช้งาน

### สำหรับ Admin

1. **เข้าสู่ระบบ**
   - เข้าหน้า `/admin/seat-swap`
   - ต้อง login ด้วย admin account

2. **ค้นหาการจอง**
   - กรอก QR Code (เช่น AIA-123456)
   - กดปุ่ม "ค้นหาการจอง"

3. **เลือกที่นั่งใหม่**
   - ดูข้อมูลการจองปัจจุบัน
   - เลือกที่นั่งใหม่จากผังที่นั่ง
   - สามารถกรองตาม Tier ได้

4. **ยืนยันการสลับ**
   - ตรวจสอบข้อมูลเปรียบเทียบ
   - กรอกเหตุผล (ไม่บังคับ)
   - กรอกบันทึกของ Admin (ไม่บังคับ)
   - กดยืนยัน

5. **ผลลัพธ์**
   - ระบบจะแจ้งผลสำเร็จหรือล้มเหลว
   - ข้อมูลถูกบันทึกใน audit log
   - อีเมลถูกส่งไปยังผู้ใช้ (ถ้าตั้งค่า)

## 🔐 ความปลอดภัย

### ที่ระดับ Database
- ✅ Atomic transactions (ทั้งหมดสำเร็จหรือ rollback ทั้งหมด)
- ✅ Foreign key constraints
- ✅ Unique constraints
- ✅ Check constraints สำหรับ status

### ที่ระดับ Application
- ✅ Role-based access control (เฉพาะ admin)
- ✅ Input validation
- ✅ Error handling
- ✅ Audit logging (บันทึกทุกการเปลี่ยนแปลง)

### ข้อจำกัดสำคัญ
- ❌ ห้ามเปลี่ยน `qr_token` (เก็บไว้เดิม)
- ❌ ห้ามสลับไปยังที่นั่งที่ถูกจองแล้ว
- ❌ ห้ามสลับไปยังที่นั่งเดิม
- ✅ ต้องมี admin authentication

## 📊 Audit Trail

ทุกการสลับที่นั่งจะถูกบันทึกด้วยข้อมูล:
- เวลาที่แม่นยำ (timestamp with timezone)
- Admin ที่ทำการสลับ
- ที่นั่งเดิมและใหม่
- เหตุผลและบันทึก
- IP address
- สถานะ (success/failed/rollback)
- Error message (ถ้ามี)

## 🔍 การดูประวัติ

### ใน Database
```sql
-- ดูประวัติทั้งหมด
SELECT * FROM seat_swap_history_detailed 
ORDER BY swapped_at DESC 
LIMIT 50;

-- ดูประวัติของ booking เฉพาะ
SELECT * FROM seat_swap_history_detailed 
WHERE booking_id = 'xxx-xxx-xxx'
ORDER BY swapped_at DESC;

-- สถิติการสลับ
SELECT 
  status,
  COUNT(*) as count
FROM seat_swap_history
GROUP BY status;
```

### ใน Application
```typescript
// ดูประวัติ
const history = await seatSwapService.getSwapHistory(50);

// ดูสถิติ
const stats = await seatSwapService.getSwapStats();
```

## 🚀 Features ที่พร้อมใช้งาน

### ✅ ทำงานแล้ว
- [x] Database schema และ migration
- [x] Atomic swap function
- [x] TypeScript types
- [x] Service layer (API calls)
- [x] Admin Portal UI
- [x] Seat map selector
- [x] Confirmation dialog
- [x] Audit logging
- [x] Error handling

### 🔄 ต้องเพิ่มเติม
- [ ] Real-time updates (Supabase Realtime)
- [ ] Email notifications (Resend API)
- [ ] Rate limiting
- [ ] Advanced filtering/search
- [ ] Export audit logs
- [ ] Swap history dashboard
- [ ] Undo swap functionality

## 📧 Email Notification (ยังไม่ได้ implement)

### Template ที่แนะนำ
```
Subject: ที่นั่งของคุณได้รับการอัพเดทแล้ว - AIA Concert

เรียน คุณ [ชื่อ],

ที่นั่งของคุณได้รับการเปลี่ยนแปลงดังนี้:

ที่นั่งเดิม: [Zone] - Row [Row], Seat [Number]
ที่นั่งใหม่: [Zone] - Row [Row], Seat [Number]

QR Code ของคุณยังคงเหมือนเดิม: [QR Code]
Booking Code: [Booking Code]

กรุณานำ QR Code นี้มาแสดงในวันงาน

ขอบคุณครับ/ค่ะ
AIA Concert Team
```

## 🔧 Troubleshooting

### ปัญหา: TypeScript errors ใน seatSwapService.ts
**สาเหตุ**: Supabase types ไม่ support RPC function parameters
**แก้ไข**: ใช้ `as any` type assertion (ทำไว้แล้ว)

### ปัญหา: Swap ล้มเหลวโดยไม่มี error message
**ตรวจสอบ**:
1. ที่นั่งใหม่ว่างอยู่หรือไม่
2. Admin มี permission หรือไม่
3. Database function ถูกสร้างแล้วหรือยัง

### ปัญหา: QR Code เปลี่ยนหลังสลับที่นั่ง
**สาเหตุ**: Logic ผิดพลาด
**แก้ไข**: ตรวจสอบว่า `swap_seat()` function ไม่ได้อัปเดท `qr_token`

## 📝 Best Practices

1. **ทดสอบก่อนใช้งานจริง**
   - ทดสอบใน development environment ก่อน
   - ทดสอบ edge cases (ที่นั่งเต็ม, invalid QR, etc.)

2. **Backup ข้อมูลก่อน Migration**
   - Export ข้อมูล bookings และ seats
   - สร้าง snapshot ของ database

3. **Monitor Audit Logs**
   - ตรวจสอบ logs เป็นประจำ
   - ติดตาม failed swaps

4. **ให้ Training แก่ Admin**
   - อธิบายขั้นตอนการใช้งาน
   - แจ้งข้อจำกัดและข้อควรระวัง

## 🎯 Next Steps

1. **Implement Real-time Updates**
   ```typescript
   // Subscribe to seat changes
   const subscription = supabase
     .channel('seats')
     .on('postgres_changes', 
       { event: '*', schema: 'public', table: 'seats' },
       (payload) => {
         // Update UI
       }
     )
     .subscribe();
   ```

2. **Add Email Notifications**
   - ตั้งค่า Resend API
   - สร้าง email template
   - เพิ่ม email sending ใน swap function

3. **Create Admin Dashboard**
   - แสดงสถิติการสลับ
   - แสดงประวัติล่าสุด
   - Export reports

4. **Add Rate Limiting**
   - จำกัดจำนวนครั้งที่ swap ได้ต่อชั่วโมง
   - ป้องกัน abuse

## 📞 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ audit logs ใน database
2. ดู console logs ใน browser
3. ตรวจสอบ Supabase logs
4. ติดต่อ development team

---

**Version**: 1.0.0  
**Last Updated**: 2026-01-21  
**Status**: Ready for Testing
