# Admin Check-in System - คู่มือการใช้งาน

## 📋 ภาพรวมระบบ

ระบบ Admin Check-in สำหรับงาน AIA Live in Concert ช่วยให้เจ้าหน้าที่ที่ประตูสามารถ:
- ตรวจสอบความถูกต้องของตั๋วผ่าน QR Code
- บันทึกการ Check-in ของผู้เข้าร่วมงาน
- ดูสถิติการ Check-in แบบ Real-time
- ดูประวัติการ Check-in ล่าสุด

---

## 🗄️ ขั้นตอนที่ 1: ติดตั้งฐานข้อมูล

### 1.1 เข้าสู่ Supabase Dashboard
1. ไปที่ [https://supabase.com](https://supabase.com)
2. เลือก Project ของคุณ
3. ไปที่ **SQL Editor**

### 1.2 รัน SQL Schema
คัดลอกและรันไฟล์ `admin-checkin-schema.sql` ใน SQL Editor:

```sql
-- ไฟล์นี้จะสร้าง:
-- ✅ ตาราง gates (ประตูต่างๆ)
-- ✅ ตาราง admin_users (เจ้าหน้าที่)
-- ✅ ตาราง check_ins (บันทึกการ check-in)
-- ✅ ข้อมูลเริ่มต้น (4 ประตู, 3 admin users)
```

### 1.3 ตรวจสอบการติดตั้ง
หลังรัน SQL แล้ว ให้ตรวจสอบว่ามีตารางเหล่านี้:
- ✅ `gates`
- ✅ `admin_users`
- ✅ `check_ins`

---

## 👤 ขั้นตอนที่ 2: Admin Users เริ่มต้น

ระบบมี Admin Users เริ่มต้น 3 คน:

| Username | Password | Role | Gate | Full Name |
|----------|----------|------|------|-----------|
| `admin` | `admin123` | admin | All Gates | Admin User |
| `staff_main` | `admin123` | staff | Main Entrance | Staff Main Gate |
| `staff_vip` | `admin123` | staff | VIP Entrance | Staff VIP Gate |

**⚠️ สำคัญ:** ในการใช้งานจริง ควรเปลี่ยนรหัสผ่านและใช้ระบบ password hashing ที่ปลอดภัย

---

## 🚀 ขั้นตอนที่ 3: เข้าถึงหน้า Admin

### 3.1 เปิดหน้า Admin Dashboard
```
http://localhost:5173/admin
```

### 3.2 Login
1. กรอก Username: `staff_main`
2. กรอก Password: `admin123`
3. กดปุ่ม **เข้าสู่ระบบ**

---

## 📱 การใช้งาน Staff Dashboard

### หน้าจอหลัก
หลังจาก Login สำเร็จ จะเห็น:

#### 1. **Total Checked-in**
- แสดงจำนวนผู้ที่ Check-in แล้ว / ทั้งหมด
- Progress bar แสดงเปอร์เซ็นต์
- สีเปลี่ยนตามเปอร์เซ็นต์:
  - 🟢 เขียว: ≥ 80%
  - 🟡 เหลือง: 50-79%
  - ⚪ เทา: < 50%

#### 2. **Verification Method**
- **QR Scanner**: กดเพื่อเปิดกล้องสแกน QR Code
- **Manual Entry**: กรอก Ticket ID ด้วยตนเอง

#### 3. **Zone Statistics**
- แสดงสถิติการ Check-in แยกตาม Tier
- PLATINUM, GOLD, SILVER, CLASSIC
- แสดงจำนวนและเปอร์เซ็นต์

#### 4. **Recent Check-ins**
- แสดงรายการ Check-in ล่าสุด 10 รายการ
- แสดงชื่อ, Tier, เวลา

---

## 📸 การสแกน QR Code

### วิธีที่ 1: ใช้กล้อง (แนะนำ)
1. กดปุ่ม **TAP TO SCAN QR CODE**
2. อนุญาตการเข้าถึงกล้อง
3. วางกล้องให้ตรงกับ QR Code บนตั๋ว
4. ระบบจะตรวจสอบอัตโนมัติ

### วิธีที่ 2: กรอกด้วยตนเอง
1. กรอก QR Token ในช่อง Manual Entry
2. กด Enter หรือกดปุ่ม **Verify**

---

## ✅ Popup Verification

หลังสแกน QR Code จะเห็น Popup แสดง:

### กรณีตั๋วถูกต้อง (Valid Ticket)
- ✅ ไอคอนเครื่องหมายถูกสีเขียว
- แสดงข้อมูล:
  - **ชื่อผู้เข้าร่วม**
  - **Tier** (PLATINUM/GOLD/SILVER/CLASSIC)
  - **Section** (ZONE A1, B2, etc.)
  - **Row** (แถว)
  - **Seat** (ที่นั่ง)
- ปุ่ม **Confirm Check-in** สีแดง

### กรณีตั๋วถูก Check-in แล้ว
- ⚠️ ไอคอนเครื่องหมายเตือนสีเหลือง
- แสดงข้อความ "Already Checked In"
- แสดงเวลาที่ Check-in
- ไม่สามารถ Check-in ซ้ำได้

### กรณีตั๋วไม่ถูกต้อง (Invalid Ticket)
- ❌ ไอคอนเครื่องหมายผิดสีแดง
- แสดงข้อความ "Invalid Ticket"
- แสดงเหตุผล (ไม่พบตั๋วในระบบ)

---

## 🔄 การ Check-in

1. หลังตรวจสอบข้อมูลถูกต้อง
2. กดปุ่ม **Confirm Check-in**
3. ระบบจะบันทึกการ Check-in
4. แสดงสถานะสำเร็จ
5. Popup ปิดอัตโนมัติ
6. Dashboard อัปเดตสถิติใหม่

---

## 📊 ข้อมูลที่บันทึก

เมื่อ Check-in สำเร็จ ระบบจะบันทึก:
- 🎫 Booking Seat ID
- 👤 Admin User ID (ผู้ทำการ Check-in)
- 🚪 Gate ID (ประตูที่ Check-in)
- ⏰ เวลาที่ Check-in
- 📝 หมายเหตุ (ถ้ามี)

---

## 🔐 การ Logout

1. กดปุ่มไอคอน Logout ที่มุมขวาบน
2. ระบบจะนำกลับไปหน้า Login
3. ข้อมูล Session จะถูกลบออก

---

## 🛠️ การจัดการ Admin Users

### เพิ่ม Admin User ใหม่
รันคำสั่ง SQL ใน Supabase:

```sql
INSERT INTO admin_users (username, password_hash, full_name, gate_id, role)
VALUES (
    'staff_new',
    '$2a$10$...', -- ใช้ bcrypt hash
    'Staff New Gate',
    'gate_id_here',
    'staff'
);
```

### เปลี่ยนรหัสผ่าน
```sql
UPDATE admin_users
SET password_hash = '$2a$10$...' -- ใช้ bcrypt hash
WHERE username = 'staff_main';
```

### ปิดการใช้งาน User
```sql
UPDATE admin_users
SET is_active = false
WHERE username = 'staff_main';
```

---

## 📈 Query สถิติ

### ดูจำนวน Check-in ทั้งหมด
```sql
SELECT COUNT(*) as total_checked_in FROM check_ins;
```

### ดู Check-in แยกตาม Tier
```sql
SELECT 
    t.name as tier_name,
    COUNT(ci.id) as count
FROM check_ins ci
JOIN booking_seats bs ON ci.booking_seat_id = bs.id
JOIN seats s ON bs.seat_id = s.id
JOIN tiers t ON s.tier_id = t.id
GROUP BY t.name, t.level
ORDER BY t.level;
```

### ดู Check-in ล่าสุด
```sql
SELECT 
    bs.first_name || ' ' || bs.last_name as attendee_name,
    t.name as tier_name,
    s.row || s.number as seat,
    ci.checked_in_at,
    au.full_name as staff_name
FROM check_ins ci
JOIN booking_seats bs ON ci.booking_seat_id = bs.id
JOIN seats s ON bs.seat_id = s.id
JOIN tiers t ON s.tier_id = t.id
JOIN admin_users au ON ci.admin_user_id = au.id
ORDER BY ci.checked_in_at DESC
LIMIT 10;
```

---

## 🐛 Troubleshooting

### ปัญหา: ไม่สามารถ Login ได้
- ✅ ตรวจสอบว่ารัน SQL schema แล้ว
- ✅ ตรวจสอบ username/password ถูกต้อง
- ✅ ตรวจสอบว่า `is_active = true`

### ปัญหา: กล้องไม่เปิด
- ✅ อนุญาตการเข้าถึงกล้องในเบราว์เซอร์
- ✅ ใช้ HTTPS หรือ localhost
- ✅ ลองใช้ Manual Entry แทน

### ปัญหา: ตั๋วไม่ถูกต้อง
- ✅ ตรวจสอบว่า QR Token ถูกต้อง
- ✅ ตรวจสอบว่ามี booking_seat ในฐานข้อมูล
- ✅ ตรวจสอบว่า qr_token ตรงกัน

### ปัญหา: สถิติไม่อัปเดต
- ✅ รีเฟรชหน้าเว็บ
- ✅ ตรวจสอบ Console สำหรับ errors
- ✅ ระบบจะ auto-refresh ทุก 10 วินาที

---

## 📁 โครงสร้างไฟล์

```
/Users/kwanjirakakate/AIA-88th-Concert/
├── admin-checkin-schema.sql          # SQL Schema
├── types/admin.ts                     # TypeScript Types
├── lib/services/adminService.ts       # API Services
├── components/admin/
│   ├── AdminLogin.tsx                 # หน้า Login
│   ├── StaffDashboard.tsx             # Dashboard หลัก
│   ├── QRScanner.tsx                  # QR Scanner
│   └── TicketVerificationModal.tsx    # Popup ตรวจสอบตั๋ว
└── pages/admin.tsx                    # หน้า Admin หลัก
```

---

## 🔒 Security Best Practices

1. **Password Hashing**: ใช้ bcrypt สำหรับ hash password
2. **HTTPS**: ใช้ HTTPS ในการใช้งานจริง
3. **Session Management**: ตั้งเวลา timeout สำหรับ session
4. **Role-based Access**: จำกัดสิทธิ์ตาม role
5. **Audit Log**: บันทึก log การเข้าถึงระบบ

---

## 📞 การติดต่อ

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อ:
- 📧 Email: support@aia-concert.com
- 📱 Tel: 02-XXX-XXXX

---

## ✨ Features

- ✅ Real-time statistics
- ✅ QR Code scanning
- ✅ Manual ticket verification
- ✅ Check-in history
- ✅ Multi-gate support
- ✅ Role-based access
- ✅ Responsive design
- ✅ Auto-refresh data

---

**สร้างโดย:** AIA Concert Team  
**เวอร์ชัน:** 1.0.0  
**อัปเดตล่าสุด:** 20 มกราคม 2026
